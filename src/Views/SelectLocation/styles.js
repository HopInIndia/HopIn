import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
	container: {
		position: 'relative',
		top:0,
		left:0,
		width: '100%',
		height: '100%'
	},
	map: {
		position: 'absolute',
		top:0,
		left:0,
		flex:1,
		width:'100%',
		height:'100%'
	},
})

export default styles;
